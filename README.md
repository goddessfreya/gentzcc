`gentzcc` aims to be a distributed compiler that caches both generated object 
files AND preprocessed files AND the macros they depend on, using two separate 
caches.

Currently, we are working on the preprocessor.
